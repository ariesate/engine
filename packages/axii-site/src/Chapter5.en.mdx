import NonLayoutStyle from "./example/NonLayoutStyle.jsx";
import NonLayoutStyleStr from "./example/NonLayoutStyle.jsx?raw";
import Code from './components/Code.jsx'
import Tips from './components/Tips.jsx'

# Non-Layout Style

Non-Layout style refer to layout irrelated style such as `color`/`font-family`.
User can write better non-layout style by implementing **feature**.

<Code instance={<NonLayoutStyle />} code={NonLayoutStyleStr}/>

<Tips content="Checkout more about feature mechanism bellow."/>

