import DynamicStructure from "./example/DynamicStructure.jsx";
import DynamicStructureStr from "./example/DynamicStructure.jsx?raw";
import Code from './components/Code.jsx'

# Dynamic Structure

Use function to represent a dynamic structure such as repeated structure or conditional structure.
(Axii will auto detect dynamic structure expression in jsx in the future, so user do not need to use function to declare.)

<Code instance={<DynamicStructure />} code={DynamicStructureStr} />
