# 创建更好的组件和组件库

使用 Axii 提供的 `createComponent` 来创建组件可获得一下能力增强：

 - 更好的样式系统
   - 纯样式逻辑与其他逻辑的分离
   - 布局样式与非布局样式的分离
 - 更强的组件行为的控制
   - 自动实现 callback
   - 自动实现 controlled/uncontrolled/delegated 模式
 - Feature based 组件逻辑拆分